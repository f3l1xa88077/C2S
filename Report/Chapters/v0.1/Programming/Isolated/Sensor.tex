Although some work was attempted like trying to read from the internal temperature sensor, the VDDIO pin mistake was discovered, so all attempts to program the board were abandoned as the STM32 would be unable to read data without a logic level shifter.